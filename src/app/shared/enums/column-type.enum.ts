export enum EColumnType {
  Text = 'text',
  Number = 'number',
  Email = 'email',
  Date = 'date',
}

export enum EColumnRenderType {
  Text = 'text',
  Link = 'link',
  Badge = 'badge',
  Currency = 'currency',
  Icon = 'icon',
}
